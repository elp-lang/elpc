import { UUID } from "elp/types"

export object User {
	.displayName: string
}

export object Message {
	.body: const string
	.user: const User
	.reactions: []string
}

export enum Actions {
	.LOGIN(user: User)
	.SEND_MESSAGE(body: string)
	.DELETE_MESSAGE(id: UUID)
	.LOGOUT
}
